const repositoryFactory = require('./repositoryFactory');

/**
  { 
    task: [ { task: 'Task name' } ],
    user: [ { user: 'Nikolas J. Rangel' } ],
    project: [ { project: 'Project name' } ] 
  }
 */
console.log(repositoryFactory);
